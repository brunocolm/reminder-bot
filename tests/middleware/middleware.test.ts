import { getDateAndReminder, sendMessage } from "../../src/middleware";

describe("getDateAndReminder", () => {
  it("should return undefined if message didn't process properly", async () => { });
  it("should return [date, reminder] if message when processed properly", async () => { });
});

describe("sendMessage", () => {
  it("", async () => { });
});
